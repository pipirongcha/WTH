(int[] numbers)
    {
        int evenSum = 0, oddSum = 0;

        foreach (int num in numbers)
        {
            if (num % 2 == 0)
                evenSum += num;
            else
                oddSum += num;
        }

        Console.WriteLine($"Even Sum: {evenSum}, Odd Sum: {oddSum}")
    }
}