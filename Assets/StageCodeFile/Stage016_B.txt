(string str)
    {
        for (int i = str.Length - 1; i >= 0; i--)
        {
         Console.Write(str[i] == 'a' ? "!" : str[i]);
         if (i % 2 == 0) Console.Write(" ");
        }
        Console.WriteLine();
    }
}