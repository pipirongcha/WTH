(string 이십오프로이상지분보유자, string 이십오프로미만지분보유자, bool 실소유자확인제외대상)
    {
        string 결과 = "결과: ";

        if (실소유자확인제외대상)
        {
            결과 += "실소유자 확인 제외 대상입니다.";
        }
        else
        {
            결과 += $"최대 지분자: {(!string.IsNullOrEmpty(이십오프로이상지분보유자) ? 이십오프로이상지분보유자 : 이십오프로미만지분보유자)}";
        }

        Console.WriteLine(결과);
    }
}