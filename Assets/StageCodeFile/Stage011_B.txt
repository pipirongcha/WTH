(int n)
    {
        string a = n.ToString();
        int b = a.Length;
        for (int i = 0; i < b / 2; i++)
        {
            if (a[i] != a[b - 1 - i])
                return false;
        }
        return true;
    }
}